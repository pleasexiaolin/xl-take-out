<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.xiaolin.mapper.ShoppingCartMapper">
    <select id="list" resultType="com.xiaolin.vo.ShoppingCartVO">
        select id,
               name,
               image,
               user_id,
               dish_id,
               setmeal_id,
               dish_flavor,
               number,
               amount,
               create_time
        from shopping_cart
        where user_id = #{userId}
    </select>

    <select id="getShoppingCartDish" resultType="com.xiaolin.vo.ShoppingCartVO">
        select id,
               name,
               image,
               user_id,
               dish_id,
               setmeal_id,
               dish_flavor,
               number,
               amount,
               create_time
        from shopping_cart
        where user_id = #{userId}
          AND dish_id = #{dishId}
        <if test="dishFlavor != null and dishFlavor != ''">
            AND dish_flavor = #{dishFlavor}
        </if>
    </select>

    <select id="getShoppingCartSetmeal" resultType="com.xiaolin.vo.ShoppingCartVO">
        select id,
               name,
               image,
               user_id,
               dish_id,
               setmeal_id,
               dish_flavor,
               number,
               amount,
               create_time
        from shopping_cart
        where user_id = #{userId}
          AND setmeal_id = #{setmealId}
    </select>

    <delete id="clean">
        delete
        from shopping_cart
        where user_id = #{userId}
    </delete>
</mapper>