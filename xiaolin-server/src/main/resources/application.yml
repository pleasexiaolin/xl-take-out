server:
  port: 9090

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${xl.datasource.driver-class-name}
      url: jdbc:mysql://${xl.datasource.host}:${xl.datasource.port}/${xl.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${xl.datasource.username}
      password: ${xl.datasource.password}
  redis:
    host: ${xl.redis.host}
    port: ${xl.redis.port}
    password: ${xl.redis.password}
    database: ${xl.redis.database}
    timeout: 5000

logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto  # 使用数据库自增
xl:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    admin-secret-key: xiaolin
    # 设置jwt过期时间
    admin-ttl: 7200000
    # 设置前端传递过来的令牌名称
    admin-token-name: token
    # C端
    user-secret-key: xiaoru
    user-ttl: 7200000
    user-token-name: authentication
  redis:
    defaultExpire: ${xl.redis.defaultExpire}
  alioss:
    endpoint: ${xl.alioss.endpoint}
    bucket-name: ${xl.alioss.bucket-name}
  wechat:
    appid: ${xl.wechat.appid}
    secret: ${xl.wechat.secret}
    login-url: https://api.weixin.qq.com/sns/jscode2session

jasypt:
  encryptor:
    password: ${JASYPT_ENCRYPTOR_PASSWORD}
    property:
      prefix: ENC(
      suffix: )